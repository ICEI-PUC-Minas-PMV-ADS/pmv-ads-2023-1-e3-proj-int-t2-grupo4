// UserContext.js

import { createContext } from "react";

const UserContext = createContext(); // Crie o contexto do usuário

export default UserContext; // Exporte o contexto do usuário para uso em outros componentes
